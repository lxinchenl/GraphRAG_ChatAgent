## 5.9日修改内容
#### 下午今日修改总结

  修改文件：src/kg_rag_demo/chunking.py

  ┌─────┬─────────────────────────────────────────────┬──────────┐
  │  #  │                  修改内容                   │   类型   │
  ├─────┼─────────────────────────────────────────────┼──────────┤
  │ 1   │ _chunk_id 调用前提取 text_content           │ Bug 修复 │
  ├─────┼─────────────────────────────────────────────┼──────────┤
  │ 2   │ split_text_recursive 重叠丢失               │ Bug 修复 │
  ├─────┼─────────────────────────────────────────────┼──────────┤
  │ 3   │ split_markdown 首部文本丢失                 │ Bug 修复 │
  ├─────┼─────────────────────────────────────────────┼──────────┤
  │ 4   │ split_markdown 合并小章节                   │ 功能优化 │
  ├─────┼─────────────────────────────────────────────┼──────────┤
  │ 5   │ split_markdown 章节间 overlap               │ 功能优化 │
  ├─────┼─────────────────────────────────────────────┼──────────┤
  │ 6   │ prev_tail 大边界重置                        │ 功能优化 │
  ├─────┼─────────────────────────────────────────────┼──────────┤
  │ 7   │ 两级 overlap 去重                           │ 功能优化 │
  ├─────┼─────────────────────────────────────────────┼──────────┤
  │ 8   │ normalize_text 保留 Markdown 强制换行       │ 功能优化 │
  ├─────┼─────────────────────────────────────────────┼──────────┤
  │ 9   │ min_chunk_size 最小 chunk 约束              │ 功能优化 │
  ├─────┼─────────────────────────────────────────────┼──────────┤
  │ 10  │ token 计数 (tiktoken cl100k_base)，回退字符 │ 功能优化 │
  └─────┴─────────────────────────────────────────────┴──────────┘

  修改文件：test_chunking.py

  ┌─────┬─────────────────────────────────────────────────┬──────────┐
  │  #  │                    修改内容                     │   类型   │
  ├─────┼─────────────────────────────────────────────────┼──────────┤
  │ 11  │ 添加 sys.stdout.reconfigure(encoding='utf-8')   │ Bug 修复 │
  ├─────┼─────────────────────────────────────────────────┼──────────┤
  │ 12  │ 添加 sys.path.insert(0, 'src') 确保使用本地代码 │ Bug 修复 │
  └─────┴─────────────────────────────────────────────────┴──────────┘
  未修改：src/kg_rag_demo/parsers.py

  你的 working tree 中有未提交的 parsers.py 改动（PDF 章节识别、Excel 多 sheet 支持等），今天没有动这个文件。

  ---
  核心变化：chunk 逻辑从"简单字符滑动窗口"升级为"语义分隔符递归切分 + token 计量 + 章节感知 + 可控 overlap"，对
  PDF、Markdown、Excel 三种格式都有了针对性的处理路径。
#### 晚上修改总结